From 41b1e965e202af8d532bdfe75ba873a862538f16 Mon Sep 17 00:00:00 2001
Message-ID: <41b1e965e202af8d532bdfe75ba873a862538f16.1735925682.git.alec@equinor.com>
From: Alena Chaikouskaya <alec@equinor.com>
Date: Fri, 3 Jan 2025 14:07:05 +0100
Subject: [PATCH] Delete pages

---
 src/OpenVDS/OpenVDS/VolumeDataAccessManager.h   |  8 +++++++-
 src/OpenVDS/VDS/VolumeDataAccessManagerImpl.cpp | 10 +++++++++-
 src/OpenVDS/VDS/VolumeDataAccessor.cpp          |  2 ++
 src/OpenVDS/VDS/VolumeDataPageAccessorImpl.cpp  | 10 ++++++++++
 src/OpenVDS/VDS/VolumeDataRequestProcessor.cpp  |  9 +++++++++
 5 files changed, 37 insertions(+), 2 deletions(-)

diff --git a/src/OpenVDS/OpenVDS/VolumeDataAccessManager.h b/src/OpenVDS/OpenVDS/VolumeDataAccessManager.h
index 284d6cb0..b5e1b2d0 100644
--- a/src/OpenVDS/OpenVDS/VolumeDataAccessManager.h
+++ b/src/OpenVDS/OpenVDS/VolumeDataAccessManager.h
@@ -27,6 +27,7 @@
 #include <OpenVDS/Exceptions.h>
 
 #include <memory>
+#include <iostream>
 
 namespace OpenVDS {
 
@@ -1050,9 +1051,13 @@ public:
   /// </returns>
   std::shared_ptr<VolumeDataPageAccessor> CreateVolumeDataPageAccessor(DimensionsND dimensionsND, int LOD, int channel, int maxPages, AccessMode accessMode, int chunkMetadataPageSize = 1024)
   {
+    std::cout << "Volume data page accessor create function called" << std::endl;
     EnsureValid();
     auto volumeDataAccessManagerInterface = (m_IVolumeDataAccessManager->AddRef(), m_IVolumeDataAccessManager);
-    return std::shared_ptr<VolumeDataPageAccessor>(m_IVolumeDataAccessManager->CreateVolumeDataPageAccessor(dimensionsND, LOD, channel, maxPages, accessMode, chunkMetadataPageSize), [volumeDataAccessManagerInterface](VolumeDataPageAccessor *volumeDataPageAccessor) { if(volumeDataPageAccessor->RemoveReference() == 0) volumeDataAccessManagerInterface->DestroyVolumeDataPageAccessor(volumeDataPageAccessor); volumeDataAccessManagerInterface->Release(); });
+    return std::shared_ptr<VolumeDataPageAccessor>(
+      m_IVolumeDataAccessManager->CreateVolumeDataPageAccessor(dimensionsND, LOD, channel, maxPages, accessMode, chunkMetadataPageSize), 
+      [volumeDataAccessManagerInterface](VolumeDataPageAccessor *volumeDataPageAccessor) {std::cout << "Checking if access manager can be deleted " << std::endl; if(volumeDataPageAccessor->RemoveReference() == 0) volumeDataAccessManagerInterface->DestroyVolumeDataPageAccessor(volumeDataPageAccessor); volumeDataAccessManagerInterface->Release(); }
+    );
   }
 
   //-----------------------------------------------------------------------------
@@ -1370,6 +1375,7 @@ public:
   }
   VolumeData3DReadAccessorR32 CreateVolumeData3DReadAccessorR32(DimensionsND dimensionsND, int LOD, int channel, int maxPages, optional<float> replacementNoValue = optional<float>())
   {
+    std::cout << "r32" << std::endl;
     EnsureValid();
     VolumeDataPageAccessor *volumeDataPageAccessor = m_IVolumeDataAccessManager->CreateVolumeDataPageAccessor(dimensionsND, LOD, channel, maxPages, AccessMode_ReadOnly);
     return VolumeData3DReadAccessorR32(m_IVolumeDataAccessManager->Create3DVolumeDataAccessorR32(volumeDataPageAccessor, replacementNoValue.value_or(volumeDataPageAccessor->GetChannelDescriptor().GetNoValue())));
diff --git a/src/OpenVDS/VDS/VolumeDataAccessManagerImpl.cpp b/src/OpenVDS/VDS/VolumeDataAccessManagerImpl.cpp
index a28a52e9..684f255a 100644
--- a/src/OpenVDS/VDS/VolumeDataAccessManagerImpl.cpp
+++ b/src/OpenVDS/VDS/VolumeDataAccessManagerImpl.cpp
@@ -35,6 +35,7 @@
 #include <assert.h>
 #include <atomic>
 #include <fmt/format.h>
+#include <iostream>
 
 namespace OpenVDS
 {
@@ -49,6 +50,7 @@ VolumeDataAccessManagerImpl::VolumeDataAccessManagerImpl(VDS &vds)
 
 VolumeDataAccessManagerImpl::~VolumeDataAccessManagerImpl()
 {
+  std::cout << "Deleting main volume data access VolumeDataAccessManagerImpl::~VolumeDataAccessManagerImpl()" << std::endl;
   if (m_uploadErrors.errors.size())
   {
     m_vds.logger.LogWarning("VolumeDataAccessManager destructor: there where upload errors");
@@ -115,11 +117,11 @@ VolumeDataAccessManagerImpl::Invalidate()
 {
   std::unique_lock<std::mutex> lock(m_mutex);
   m_invalidated = true;
-  m_requestProcessor.reset();
   for (auto volumeDataPageAccessor = m_volumeDataPageAccessorList.GetFirstItem(); volumeDataPageAccessor; volumeDataPageAccessor = volumeDataPageAccessor->m_volumeDataPageAccessorListNode.m_next)
   {
     volumeDataPageAccessor->Invalidate();
   }
+  m_requestProcessor.reset();
 }
 
 void
@@ -370,6 +372,9 @@ VolumeDataAccessManagerImpl::CreateVolumeDataPageAccessor(VolumeDataLayer const
     }
   }
 
+  std::cout << "Volume data page accessor created" << std::endl;
+
+
   VolumeDataPageAccessorImpl *pageAccessor = new VolumeDataPageAccessorImpl(this, parentVolumeDataPageAccessor, volumeDataLayer, format, useNoValue, noValue, maxPages, accessMode, m_vds.logger);
   return pageAccessor;
 }
@@ -424,12 +429,14 @@ VolumeDataAccessManagerImpl::CreateVolumeDataPageAccessorConversionParam(Dimensi
 
   VolumeDataPageAccessorImpl *pageAccessor = CreateVolumeDataPageAccessor(ValidateProduceStatus(volumeDataLayer), format, useNoValue, noValue, maxPages, accessMode);
   m_volumeDataPageAccessorList.InsertLast(pageAccessor);
+  std::cout << "Volume data page accessor created through params and inserted to the list" << std::endl;
   return pageAccessor;
 }
 
 VolumeDataPageAccessor *
 VolumeDataAccessManagerImpl::CreateVolumeDataPageAccessor(DimensionsND dimensionsND, int LOD, int channel, int maxPages, VolumeDataAccessManager::AccessMode accessMode, int chunkMetadataPageSize)
 {
+  std::cout << "Create volume data page acessor" << std::endl;
   VolumeDataLayer* volumeDataLayer;
   {
     std::unique_lock<std::mutex> lock(m_mutex);
@@ -441,6 +448,7 @@ VolumeDataAccessManagerImpl::CreateVolumeDataPageAccessor(DimensionsND dimension
 void
 VolumeDataAccessManagerImpl::DestroyVolumeDataPageAccessor(VolumeDataPageAccessor *volumeDataPageAccessor)
 {
+  std::cout << "Destroy volume data page accessor" << std::endl;
   std::unique_lock<std::mutex> lock(m_mutex);
   VolumeDataPageAccessorImpl *pageAccessor = static_cast<VolumeDataPageAccessorImpl *>(volumeDataPageAccessor);
   m_volumeDataPageAccessorList.Remove(pageAccessor);
diff --git a/src/OpenVDS/VDS/VolumeDataAccessor.cpp b/src/OpenVDS/VDS/VolumeDataAccessor.cpp
index c6b69df5..78774c01 100644
--- a/src/OpenVDS/VDS/VolumeDataAccessor.cpp
+++ b/src/OpenVDS/VDS/VolumeDataAccessor.cpp
@@ -27,6 +27,7 @@
 #include <inttypes.h>
 
 #include <fmt/format.h>
+#include <iostream>
 
 namespace OpenVDS
 {
@@ -124,6 +125,7 @@ VolumeDataAccessorBase::~VolumeDataAccessorBase()
     m_currentPage->Release();
     m_currentPage = nullptr;
   }
+  std::cout << "Volume Data Accessor base destructor" << std::endl;
   if(m_volumeDataPageAccessor)
   {
     if(m_volumeDataPageAccessor->RemoveReference() == 0)
diff --git a/src/OpenVDS/VDS/VolumeDataPageAccessorImpl.cpp b/src/OpenVDS/VDS/VolumeDataPageAccessorImpl.cpp
index 78b9c86a..d51fba06 100644
--- a/src/OpenVDS/VDS/VolumeDataPageAccessorImpl.cpp
+++ b/src/OpenVDS/VDS/VolumeDataPageAccessorImpl.cpp
@@ -34,6 +34,7 @@
 #include <algorithm>
 
 #include <fmt/printf.h>
+#include <iostream>
 
 namespace OpenVDS
 {
@@ -68,12 +69,21 @@ VolumeDataPageAccessorImpl::VolumeDataPageAccessorImpl(VolumeDataAccessManagerIm
 
 VolumeDataPageAccessorImpl::~VolumeDataPageAccessorImpl()
 {
+  std::cout << "De-struc-tor!! De-struc-tor!" << std::endl;
   m_pages.clear();
   while(auto page = m_pages_lru.GetFirstItem())
   {
     m_pages_lru.Remove(page);
     delete page;
   }
+  for (auto it = m_pages.begin(); it != m_pages.end(); ++it)
+  {
+    if (it->second)
+    {
+      std::cerr << "Page not deleted" << std::endl;
+    }
+  }
+  std::cout << "Delete iteration over" << std::endl;
 
   if(m_parentVolumeDataPageAccessor)
   {
diff --git a/src/OpenVDS/VDS/VolumeDataRequestProcessor.cpp b/src/OpenVDS/VDS/VolumeDataRequestProcessor.cpp
index f795dbe7..541454f0 100644
--- a/src/OpenVDS/VDS/VolumeDataRequestProcessor.cpp
+++ b/src/OpenVDS/VDS/VolumeDataRequestProcessor.cpp
@@ -40,6 +40,7 @@
 
 #include <fmt/format.h>
 #include <exception>
+#include <iostream>
 
 namespace OpenVDS
 {
@@ -1699,6 +1700,7 @@ static void CleanupThread(PageAccessorNotifier &pageAccessorNotifier,  std::map<
 {
   auto long_block = std::chrono::hours(24 * 32 * 12);
   auto in_progress_block = std::chrono::seconds(_cleanupthread_timeoutseconds);
+  std::cout << "Cleanup thread created" << std::endl;
   while(!pageAccessorNotifier.exit)
   {
     std::unique_lock<std::mutex> lock(pageAccessorNotifier.mutex);
@@ -1749,6 +1751,12 @@ VolumeDataRequestProcessor::~VolumeDataRequestProcessor()
     job->cancelled = true;
   m_pageAccessorNotifier.setExit();
   m_cleanupThread.join();
+  std::cout << "I am trying to delete something, not sure what" << std::endl;
+  for (auto& accessor : m_pageAccessors)
+  {
+    delete accessor.second;
+  }
+  m_pageAccessors.clear();
 }
 
 static int64_t GenJobId()
@@ -1847,6 +1855,7 @@ int64_t VolumeDataRequestProcessor::AddJob(const std::vector<VolumeDataChunk>& c
   auto page_accessor_it = m_pageAccessors.find(key);
   if (page_accessor_it == m_pageAccessors.end())
   {
+    std::cout << "So you are created in job" << std::endl;
     auto pa = m_manager.CreateVolumeDataPageAccessorConversionParam(dimensions, lod, channel, format, noValue.first, noValue.second, maxPages, VolumeDataAccessManager::AccessMode_ReadOnly, 1024);
     pa->RemoveReference();
     auto insert_result = m_pageAccessors.emplace(key, pa);
-- 
2.43.0

